export const metadata = {
    title: 'Projects',
    description: 'Production systems and personal builds — from e-commerce at scale to full-stack experiments.',
}

export default function ProjectsLayout({ children }) {
    return children
}
